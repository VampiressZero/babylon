/** Urls of textures. */
export interface TexturesUrl {

  /** Url of displacement. */
  readonly displacement: string;

  /** Url of normal. */
  readonly normal: string;

  /** Url of AO. */
  readonly ao: string;

  /** Url of rough. */
  readonly rough: string;

  /** Url of diffuse. */
  readonly diffuse: string;
}
